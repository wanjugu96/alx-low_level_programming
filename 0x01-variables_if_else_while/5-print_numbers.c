#include <stdlib.h>
#include <stdio.h>
#include <ctype.h>
/**
 * main - print numbers
 * Return: 0
 */
int main(void)
{
    int num = 0;
    for (num=0; num <= 9; num++)
    {
        printf("%d\n", num);
    }
    return (0);
}
